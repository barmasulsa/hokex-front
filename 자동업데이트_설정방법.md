# 방문자 통계 자동 업데이트 설정 방법

## 문제
밤 12시가 지나도 통계가 자동으로 업데이트되지 않음

## 해결 방법

### 1단계: Supabase 접속
1. https://supabase.com 접속
2. 프로젝트 선택
3. 왼쪽 메뉴에서 **SQL Editor** 클릭

### 2단계: SQL 실행
1. `setup-auto-update-trigger.sql` 파일 열기
2. 파일 내용 **전체 복사** (Ctrl+A → Ctrl+C)
3. Supabase SQL Editor에 **붙여넣기** (Ctrl+V)
4. **Run** 버튼 클릭 (또는 Ctrl+Enter)

### 3단계: 결과 확인
실행 결과에 다음과 같이 표시되면 성공:

```
✅ 캐시 업데이트 완료
오늘: XX
어제: XX
최근7일: XX
최근30일: XX
업데이트시간: 2026-05-29 ...
```

그리고 트리거 설정 확인 결과가 표시됩니다.

## 작동 원리

### 자동 업데이트
- 사용자가 홈페이지 방문 → `visitor_stats` 테이블에 데이터 추가
- **Database Trigger 자동 실행** → 캐시 즉시 업데이트
- 프론트엔드는 캐시에서 빠르게 통계 조회

### 날짜 변경 시
- 밤 12시가 지나면 → 첫 방문자 접속 시
- 새로운 날짜로 데이터 추가 → Trigger 자동 실행
- **"오늘"과 "어제" 통계 자동 갱신**

## 장점
✅ **Cron Job 불필요** - Database Trigger만으로 작동  
✅ **즉시 반영** - 방문자가 있을 때마다 자동 업데이트  
✅ **안정적** - Database Trigger는 매우 신뢰성 높음  
✅ **날짜 변경 자동 처리** - 밤 12시 이후 첫 방문자가 자동으로 통계 갱신

## 주의사항
- 방문자가 없는 날에는 캐시가 업데이트되지 않음
- 하지만 다음 방문자가 오면 자동으로 최신 통계로 업데이트됨
- 성능 영향은 거의 없음 (캐시 테이블은 단일 행만 업데이트)

## 문제 해결

### 수동으로 캐시 강제 업데이트
만약 통계가 이상하게 보이면 Supabase SQL Editor에서 실행:

```sql
WITH stats AS (
  SELECT 
    COALESCE(SUM(CASE WHEN visit_date = CURRENT_DATE THEN visit_count ELSE 0 END), 0) as today_count,
    COALESCE(SUM(CASE WHEN visit_date = CURRENT_DATE - INTERVAL '1 day' THEN visit_count ELSE 0 END), 0) as yesterday_count,
    COALESCE(SUM(CASE WHEN visit_date >= CURRENT_DATE - INTERVAL '7 days' THEN visit_count ELSE 0 END), 0) as last_7_days_count,
    COALESCE(SUM(CASE WHEN visit_date >= CURRENT_DATE - INTERVAL '30 days' THEN visit_count ELSE 0 END), 0) as last_30_days_count
  FROM visitor_stats
)
UPDATE visitor_stats_cache
SET 
  today = (SELECT today_count FROM stats),
  yesterday = (SELECT yesterday_count FROM stats),
  last_7_days = (SELECT last_7_days_count FROM stats),
  last_30_days = (SELECT last_30_days_count FROM stats),
  updated_at = NOW()
WHERE cache_key = 'summary'
RETURNING today, yesterday, last_7_days, last_30_days, updated_at;
```

## 완료!
이제 통계가 자동으로 실시간 업데이트됩니다. 🎉
