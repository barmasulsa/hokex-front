import type { EventRecord } from '../types/core';
import { Region, Category } from '../types/core';
import { generateDayString } from '../utils/dayStringGenerator';

// 테스트용 목 데이터 (빈 배열)
export const mockEvents: EventRecord[] = [];
