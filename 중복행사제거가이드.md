# 중복 행사 제거 가이드

## 문제 상황
같은 제목과 날짜를 가진 행사가 여러 번 표시되는 문제가 발생했습니다.

## 해결 방법

### 1단계: Supabase SQL Editor 접속
1. [Supabase 대시보드](https://supabase.com/dashboard) 접속
2. 프로젝트 선택 (qmhxnxnaawtjelqlgyig)
3. 왼쪽 메뉴에서 **SQL Editor** 클릭

### 2단계: 중복 확인
다음 쿼리를 실행하여 중복된 행사를 확인하세요:

```sql
SELECT title, start_date, end_date, COUNT(*) as duplicate_count
FROM events
GROUP BY title, start_date, end_date
HAVING COUNT(*) > 1
ORDER BY duplicate_count DESC;
```

### 3단계: 중복 제거
중복된 행사 중 가장 오래된 것만 남기고 나머지를 삭제합니다:

```sql
DELETE FROM events a
USING events b
WHERE a.id > b.id
  AND a.title = b.title
  AND a.start_date = b.start_date
  AND a.end_date = b.end_date;
```

**주의**: 이 쿼리는 실행 후 되돌릴 수 없습니다!

### 4단계: 중복 방지 설정
앞으로 중복이 생기지 않도록 유니크 인덱스를 추가합니다:

```sql
CREATE UNIQUE INDEX IF NOT EXISTS idx_events_unique_title_dates 
ON events(title, start_date, end_date);
```

### 5단계: 결과 확인
다음 쿼리로 중복이 제거되었는지 확인하세요:

```sql
SELECT COUNT(*) as total_events FROM events;

SELECT title, start_date, end_date, COUNT(*) as count
FROM events
GROUP BY title, start_date, end_date
ORDER BY title;
```

모든 행사의 count가 1이어야 합니다.

### 6단계: 웹사이트 확인
브라우저에서 http://localhost:5173 을 새로고침하여 중복이 사라졌는지 확인하세요.

## 완료!
이제 같은 행사가 한 번만 표시됩니다. 앞으로 같은 제목과 날짜를 가진 행사를 추가하려고 하면 데이터베이스가 자동으로 막아줍니다.
