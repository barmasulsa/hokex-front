# ✅ HOKEX 테스트 서버 실행 체크리스트

## 🎯 목표
중복 행사를 제거하고 깨끗한 테스트 서버 실행하기

---

## 📋 실행 순서

### ✅ 1. 개발 서버 확인
- [x] 프론트엔드 서버 실행 중 (http://localhost:5173)
- [x] Supabase 연결 완료

### ⚠️ 2. Supabase 중복 제거 (지금 해야 함!)

#### 2-1. Supabase 접속
1. 브라우저에서 https://supabase.com/dashboard 열기
2. 프로젝트 선택: `qmhxnxnaawtjelqlgyig`
3. 왼쪽 메뉴에서 **SQL Editor** 클릭

#### 2-2. 중복 확인 (먼저 확인만)
SQL Editor에 다음 코드 붙여넣고 **Run** 클릭:

```sql
SELECT title, start_date, end_date, COUNT(*) as duplicate_count
FROM events
GROUP BY title, start_date, end_date
HAVING COUNT(*) > 1
ORDER BY duplicate_count DESC;
```

**결과**: 중복된 행사 목록이 표시됨

#### 2-3. 중복 제거 (실행!)
SQL Editor에 다음 코드 붙여넣고 **Run** 클릭:

```sql
DELETE FROM events a
USING events b
WHERE a.id > b.id
  AND a.title = b.title
  AND a.start_date = b.start_date
  AND a.end_date = b.end_date;
```

**결과**: "Success. No rows returned" 메시지 표시

#### 2-4. 중복 방지 설정
SQL Editor에 다음 코드 붙여넣고 **Run** 클릭:

```sql
CREATE UNIQUE INDEX IF NOT EXISTS idx_events_unique_title_dates 
ON events(title, start_date, end_date);
```

**결과**: "Success. No rows returned" 메시지 표시

#### 2-5. 최종 확인
SQL Editor에 다음 코드 붙여넣고 **Run** 클릭:

```sql
SELECT COUNT(*) as total_events FROM events;
```

**결과**: 6개 행사만 남아있어야 함 (중복 제거 전보다 적음)

### ✅ 3. 웹사이트 테스트

#### 3-1. 브라우저 새로고침
- http://localhost:5173 페이지 새로고침 (Ctrl + Shift + R)

#### 3-2. 홈페이지 확인
- [ ] 행사 목록이 표시되는가?
- [ ] 중복 행사가 없는가? (같은 제목이 여러 번 나오지 않음)
- [ ] 6개 행사가 표시되는가?

#### 3-3. 필터 테스트
- [ ] 지역 필터: "서울" 클릭 → 서울 행사만 표시
- [ ] 전시장 필터: "코엑스" 클릭 → 코엑스 행사만 표시
- [ ] 연도 필터: "2026" 선택 → 2026년 행사만 표시
- [ ] 카테고리 필터: "전시" 클릭 → 전시 행사만 표시

#### 3-4. 행사 상세 페이지
- [ ] 행사 카드 클릭 → 상세 페이지로 이동
- [ ] 행사 정보가 표시되는가?
- [ ] 뒤로가기 버튼 작동하는가?

#### 3-5. 관리자 모드
- [ ] 헤더에서 "관리자 모드" 토글 ON
- [ ] 행사 정보 클릭 시 편집 가능한가?
- [ ] 토글 OFF 시 편집 불가능한가?

---

## 🎉 완료!

모든 체크박스에 체크가 되었다면 테스트 서버가 정상적으로 작동하는 것입니다!

---

## 🔧 문제 발생 시

### 중복이 여전히 보임
→ 2-3 단계 (중복 제거 SQL) 다시 실행
→ 브라우저 캐시 삭제 (Ctrl + Shift + R)

### 행사가 하나도 안 보임
→ Supabase Table Editor에서 events 테이블 확인
→ 데이터가 있는지 확인
→ `supabase-schema.sql`의 INSERT 문 다시 실행

### 필터가 안 됨
→ 브라우저 콘솔 (F12) 열어서 에러 확인
→ 에러 메시지 복사해서 문의

---

## 📞 다음 단계

1. ✅ 중복 제거 완료
2. ✅ 기본 기능 테스트 완료
3. ⬜ 실제 행사 데이터 추가
4. ⬜ 사용자 인증 구현
5. ⬜ 행사 저장 기능 구현
